# Benchmark Notes

Use this directory for small, repeatable performance checks that compare CLI startup, preset loading, and export generation without changing product behavior.

Keep benchmark inputs tiny, deterministic, and easy to rerun from local development or CI.
