# Preset web-app primary CTA result-card note

Keep the first web-app demo scoped to one primary action and one visible result card.

- One form submit button should own the happy path.
- One result card should confirm the outcome above the fold.
- Secondary links and exports can wait until the primary proof is stable.
