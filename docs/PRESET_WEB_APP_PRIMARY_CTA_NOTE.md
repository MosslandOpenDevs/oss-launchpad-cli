# Preset web-app primary CTA note

Keep the first web-app result card centered on one obvious primary call to action, then place one lighter download/export action beside it so the UI proof stays easy to scan and demo.
