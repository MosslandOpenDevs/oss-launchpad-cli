# Preset web-app result card above-fold check

Use this before widening the first generated web demo beyond one form and one result card.

Above-the-fold checklist:
- input summary is visible without scrolling
- primary status or recommendation badge is visible immediately
- one download/export target is visible from the same first card
- secondary sections can wait below the fold until the first proof stays stable
