# Preset JSON result-card scan start

For the first web-app proof, keep the result card easy to scan before adding more controls.

Check this order:
- preset label
- one-line preset summary
- validation command badge
- one obvious export/download target

This keeps the first form -> result-card slice aligned with a UI/UX-first, one-primary-path workflow.
