# Preset Web-App JSON Chooser Note

Start with `oss-launchpad presets --json --preset web-app` when the first believable proof is one form, one primary action, and one result card.
Keep the slice small enough that the exported preset metadata, review command, and starter assets still point to the same first UI proof.
