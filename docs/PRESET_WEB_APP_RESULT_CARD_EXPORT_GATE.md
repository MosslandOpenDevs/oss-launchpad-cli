# Preset web-app result-card export gate

For the web-app preset, keep the first believable proof limited to one form, one primary action, one reviewable result card, and one export/checkpoint path that can be revalidated before widening the browser surface.
