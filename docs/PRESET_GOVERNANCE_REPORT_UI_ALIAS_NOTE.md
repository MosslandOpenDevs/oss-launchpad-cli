# preset governance report ui alias note

Use `governance-report-ui` as a `web-app` preset alias when the first proof still stays one form, one primary action, and one reviewable result card with a visible report download target.
