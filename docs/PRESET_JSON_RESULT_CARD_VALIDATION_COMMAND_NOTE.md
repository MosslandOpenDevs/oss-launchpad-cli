# Preset JSON result-card validation-command note

In the first browser proof for `oss-launchpad presets --json`, keep the exported `validation_command` visible on the same result card as:

- the chosen preset label
- the short preset summary
- one explicit export or copy target

That keeps the UI slice honest: one form/chooser, one result card, one command the reviewer can rerun immediately.
