# Preset web-app result-card starter docs + export note

For the first `web-app` demo slice, keep one chooser, one result card, the starter-doc list, and one explicit export/download target visible together so the maintainer can validate both setup guidance and proof handoff in the same review pass.
