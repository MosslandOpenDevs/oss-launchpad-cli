# Preset web-app result card download stability note

For the first web demo slice, keep the flow limited to one form, one primary action, one stable result card, and one obvious export/download path before widening the UI.
