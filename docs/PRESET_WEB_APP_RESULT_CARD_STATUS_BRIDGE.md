# PRESET_WEB_APP_RESULT_CARD_STATUS_BRIDGE

Use this note when the first generated `web-app` proof already has one believable result card and needs a short maintainer update.

- Name the single visible form input that produced the card.
- Name the result card artifact or download target.
- Keep the status scoped to the first proof slice, not the whole product.
