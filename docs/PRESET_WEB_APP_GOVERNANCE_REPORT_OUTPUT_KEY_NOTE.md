# Preset web-app governance report_output_key note

For the first governance web-demo/result-card slice, treat `report_output_key` as the same stable report-bundle basename cue as `report_output_title`, `report_output_ref`, or `report_output_dir`. Keep the UI scope narrow: one scenario file, one primary action, one reviewable result card, and one visible report export target.
