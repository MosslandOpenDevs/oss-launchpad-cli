# Preset web-app result card bundle scope

Keep the first `web-app` proof intentionally narrow:

1. One form submission
2. One result card
3. One shared report bundle path

Do not widen the slice until the same run can show the result card and point to the exact downloadable artifact bundle without extra operator explanation.
