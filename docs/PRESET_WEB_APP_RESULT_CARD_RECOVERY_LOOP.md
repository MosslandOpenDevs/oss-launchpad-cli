# Preset web-app result-card recovery loop

For the first web-app demo, keep the proof loop small and reviewable:
- one form
- one primary action
- one stable result card
- one replay path after a failed browser check

Recovery loop:
1. reproduce with the smallest input set
2. recover the form-to-card happy path
3. rerun the same export/download proof before widening the UI surface
