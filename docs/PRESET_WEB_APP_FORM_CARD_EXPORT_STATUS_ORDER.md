# preset web-app form card export status order

When reviewing the first web-app proof slice, keep the visible order stable:

1. chosen preset
2. validation command
3. export or download target
4. honest progress status

That order keeps the result card readable before the demo grows wider.
