# Preset Web App Playwright Stability Loop

Use this note before widening the first `web-app` proof slice.

- Keep the UI scope narrow: one form, one result card, one proof path.
- Verify with stable selectors and replayable steps before polishing copy.
- Recover from failures by fixing the smallest broken checkpoint and rerunning the same path.
