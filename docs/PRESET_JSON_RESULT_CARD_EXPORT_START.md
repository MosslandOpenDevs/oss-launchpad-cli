# Preset JSON result card export start

For the first preset-JSON browser proof, keep the flow narrow: one setup form, one result card, and one obvious export/download action.

If the result card cannot point to a single believable export target yet, keep polishing that slice before widening the demo surface.
