# Preset JSON chooser validation export note

Keep the first preset JSON chooser tied to one validation command and one exported setup path so the result card proves both what to run and what to download next.
