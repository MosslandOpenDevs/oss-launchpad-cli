# Preset web-app governance report-output-tag note

For the first governance web demo, keep the UI small and reviewable: one scenario entry surface, one unmistakable primary action, one stable result card, and one visible export cue that may label the generated report bundle with `report_output_tag`.

Treat the same result card as reviewable only after replaying the validator-backed flow again.
