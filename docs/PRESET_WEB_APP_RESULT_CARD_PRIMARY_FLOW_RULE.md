# Preset web-app result card primary flow rule

For the first generated `web-app` proof, keep the result card centered on one obvious primary action, one visible validation command, and one believable export or download target.

Apply this before adding secondary controls so the first UI slice stays reviewable, demoable, and Playwright-friendly.
