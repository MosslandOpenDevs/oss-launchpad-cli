# Preset web-app Playwright stability note

Keep the first browser proof limited to **one form, one result card, and one downloadable artifact**. Preserve **one primary action** and recover browser runs by **replaying the last stable step** before widening scope.
