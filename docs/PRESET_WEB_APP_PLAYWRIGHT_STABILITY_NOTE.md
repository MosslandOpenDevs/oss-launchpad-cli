# Preset web app Playwright stability note

Keep the first browser-proof lane reproducible: lock the flow to one form, one result card, and one downloadable artifact, verify each checkpoint before moving forward, and recover by replaying the last stable step instead of widening scope mid-failure.
