# Preset web-app result-card status line

Use one short maintainer status line after the first web-app proof passes:

- mention one form input,
- mention one visible result card,
- mention one downloadable artifact or handoff path.

Keep it small enough that the same proof can be rerun without guessing scope.
