# Preset web-app governance proposal input markdown path note

Use this note when the first believable governance web proof still stays form -> action -> result card:
- let the backing governance-sandbox scenario load proposal body text from `proposal_input_markdown_path`,
- keep the visible browser proof scoped to one primary action and one stable result card,
- keep the report-download checkpoint in the same validator-backed pass before widening the UI.
