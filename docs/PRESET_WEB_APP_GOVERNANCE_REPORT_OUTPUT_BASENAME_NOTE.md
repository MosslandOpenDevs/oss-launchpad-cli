# PRESET_WEB_APP_GOVERNANCE_REPORT_OUTPUT_BASENAME_NOTE

Use `report_output_basename` when the governance web demo should keep one explicit scenario-file-driven bundle name across JSON, Markdown, and HTML exports.

Keep the first UI slice small:
- one scenario input
- one primary action
- one stable result card
- one exported report bundle

Validate the same preset JSON lane before widening the demo.
