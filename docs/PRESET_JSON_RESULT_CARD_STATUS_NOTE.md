# preset JSON result-card status note

When `oss-launchpad presets --json --preset web-app` feeds a form-driven starter flow, keep the first UI slice narrow: one preset chooser, one visible result card, and one status line that points reviewers to the validation command before any secondary actions.
