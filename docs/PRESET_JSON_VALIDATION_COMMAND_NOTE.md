# Preset JSON Validation Command Note

When you turn `oss-launchpad presets --json` into a small chooser or setup form, keep the exported `validation_command` visible near the first result card.
That gives the reviewer one obvious reproducible check after choosing a preset, without making them scan the full README.
For the first browser proof, pair one preset label, one validation command, and one visible next step before adding secondary actions.
