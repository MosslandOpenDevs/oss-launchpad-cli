# Preset JSON Result Card Owner Queue Note

For the first web-app demo slice, keep the exported preset JSON rich enough to power one intro-first form and one owner-facing result card.

The result card should be able to show:

- preset label
- owner/setup command
- validation command
- first UI slice
- UI/UX lane
- Playwright lane

That keeps the first browser proof aligned with one form, one primary action, and one stable result card.
