# Preset JSON result-card progress badge note

For the first web result card, expose progress in the smallest honest way:

- show one visible validation/review badge
- keep that badge tied to the same export bundle the card offers for download
- avoid adding multiple progress states before the first owner handoff is stable

This keeps the UI readable and the handoff easy to verify.
