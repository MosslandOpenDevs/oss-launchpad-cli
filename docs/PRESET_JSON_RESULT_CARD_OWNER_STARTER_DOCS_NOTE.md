# Preset JSON result card owner starter docs note

Keep the preset owner, starter-doc handoff, and validation command visible together on the first result card so one maintainer can move from preset selection to the next proof doc without opening a second screen.
