# UI/UX Checklist

Use this checklist before shipping the first `web-app` preset demo:

- Intro-first headline is visible without scrolling.
- Only one primary action competes for attention.
- The result card is readable at a glance.
- Validation or export status is visible near the card.
- The demo path can be replayed in one short deterministic pass.
