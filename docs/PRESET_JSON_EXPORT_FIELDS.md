# Preset JSON export fields

`oss-launchpad presets --json` should stay easy to consume from CI, demos, and small web forms.

Current baseline fields per preset:
- `label`
- `summary`

Use these fields to render a compact chooser before deeper preset-specific docs open.
