# Preset Web App Result Card Export Stability Note

For the first believable web-app proof, keep one form, one primary CTA, and one export-ready result card stable before widening the flow. Re-run the same small slice with step-by-step Playwright recovery so UI proof stays reproducible.
