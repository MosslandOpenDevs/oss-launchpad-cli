# Preset JSON governance JSON5 compat note

For governance-aligned `web-app` starter work, keep the first form -> primary action -> result-card slice narrow enough that commented `.json5` scenario fixtures can feed the same UI proof without changing the validation/export gate.
