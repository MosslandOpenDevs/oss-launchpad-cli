# Preset web-app governance reports_directory validate note

Keep the first governance web-app result card small: show one scenario source, one `reports_directory` export target, and one validation rerun before any commit/push claim.
