# Preset web app result-card primary action rule

Keep the first result card obvious and reviewable.

- Show one primary action before secondary links.
- Keep the validation command and export/download target visible in the same card.
- Reuse stable labels so UI checks and browser replays stay reproducible across iterations.
