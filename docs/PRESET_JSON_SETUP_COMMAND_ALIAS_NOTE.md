# Preset JSON setup command alias note

Use `setup_command` when a small chooser, setup form, or result card wants one short machine-readable field for the first customization command without dropping the fuller `customize_first_command` key.
