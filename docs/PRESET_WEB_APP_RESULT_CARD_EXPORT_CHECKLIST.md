# Preset web-app result card export checklist

Keep the first `web-app` browser proof narrow and reproducible:

1. choose one preset,
2. verify one result card,
3. verify one export or download target,
4. rerun the same proof before widening the flow.

This checklist exists to keep UI work reviewable before broader multi-step setup logic lands.
