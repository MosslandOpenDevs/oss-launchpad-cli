# Preset web-app governance reports_directory note

For the first governance web demo, keep the UI scope narrow: one scenario source, one primary action, one result card, and one visible `reports_directory` export target for the generated report bundle.

That keeps the UI/UX story simple and gives Playwright-style review a stable file-output checkpoint.
