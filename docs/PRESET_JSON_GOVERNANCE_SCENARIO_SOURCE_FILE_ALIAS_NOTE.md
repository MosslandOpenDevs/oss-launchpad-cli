# Preset JSON governance scenario_source_file alias note

For the first governance web-demo/result-card slice, accept `scenario_source_file` as the same replayable scenario-file pointer used by `source_file`/`source_path`. Keep the UI handoff narrow: one form, one primary action, one result card, and one visible report-bundle/export path.
