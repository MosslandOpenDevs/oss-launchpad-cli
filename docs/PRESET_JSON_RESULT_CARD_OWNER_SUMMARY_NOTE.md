# Preset JSON result card owner + summary note

For the first preset-driven setup card, keep three things visible together: the preset owner, the one-line preset summary, and the validation command. That trio makes the card feel explainable before you add wider UI states.
