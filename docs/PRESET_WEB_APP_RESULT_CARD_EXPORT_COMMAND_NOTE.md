# Preset web-app result card export command note

Keep the first believable web-app proof narrow: one result card, one explicit export path, and one printed validation command that confirms the generated scaffold still matches the chosen preset.
