# Preset web-app result-card recheck gate

Before widening the first web-app demo slice, keep the proof loop narrow:

1. one chooser
2. one primary action
3. one stable result card
4. one validation replay before commit/push

This keeps the governance-aligned UI proof small enough to review and reproduce.
