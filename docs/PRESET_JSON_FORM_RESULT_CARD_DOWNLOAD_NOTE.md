# Preset JSON form result-card download note

For the first web-app preset proof, keep one chooser, one visible result card, and one obvious download/export target in the same reviewable slice.

That keeps UI copy, validation cues, and artifact handoff aligned before widening the demo.
