# Web-app governance reports_directory status note

Keep the first governance demo proof narrow:
- one scenario-aware input form
- one primary action
- one reviewable result card
- one visible `reports_directory` / report bundle destination

This note is for short maintainer status lines after the validator rerun passes.
