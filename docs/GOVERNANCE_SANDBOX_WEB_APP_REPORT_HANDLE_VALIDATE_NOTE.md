# Governance-sandbox web app report handle validate note

For repo 4 and repo 5 paired passes, repo 4 should stay visibly active even while repo 5 is still scenario-file/report-first.

If repo 5 gains one more report bundle alias for a web result-card flow, keep the paired slice small:

1. add the alias
2. validate the CLI replay
3. keep the report output path visible for handoff
4. commit and push only after validation passes
