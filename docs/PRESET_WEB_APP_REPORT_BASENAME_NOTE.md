# Preset web-app report basename note

When the first visible web-app proof ends in a report download, keep the result card and downloadable artifacts on one obvious basename.

Use this rule of thumb:

- one form submission
- one result card
- one shared report basename across JSON/markdown/HTML outputs

That keeps browser proof, maintainer review, and artifact reopening aligned before widening the demo surface.
