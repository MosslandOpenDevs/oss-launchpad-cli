# Preset JSON result card owner + validation note

For the first preset-JSON result card, keep three things above the fold: the preset label, the preset-specific validation command, and the visible owner. That keeps the card scannable for maintainers while preserving one obvious next action for the generated scaffold.
