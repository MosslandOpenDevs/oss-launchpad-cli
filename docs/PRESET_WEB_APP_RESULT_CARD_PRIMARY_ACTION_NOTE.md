# Preset web-app result card primary action note

For the first generated web-app proof, keep exactly one primary action on the result card so the UI stays scannable and the browser replay stays reproducible.
