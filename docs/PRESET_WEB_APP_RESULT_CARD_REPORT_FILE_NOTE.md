# Web-app result-card report file note

Use this note when the first web-app proof needs one clear report/download file attached to a single reviewable result card. Keep the flow to one form, one result card, and one obvious exported artifact before widening the UI.
