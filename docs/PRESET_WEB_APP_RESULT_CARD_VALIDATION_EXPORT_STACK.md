# Preset web-app result card validation export stack

For the first believable web-app proof, keep the result card centered on one validation cue and one export cue.

The smallest reviewable stack is:

- one form submit
- one generated result card
- one visible validation command
- one explicit export or download target

If the card needs multiple competing actions, tighten the first slice before widening the demo.
