# Preset web-app governance source-path report-basename note

For the first governance web-demo slice, keep one visible scenario source path and one visible report basename on the first result card so the form, export path, and review handoff stay reproducible.
