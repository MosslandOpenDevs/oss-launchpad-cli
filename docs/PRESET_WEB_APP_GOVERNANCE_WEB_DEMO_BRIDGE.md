# Preset web-app governance web-demo bridge

Bridge the first governance-sandbox web demo from `oss-launchpad presets --json --preset web-app`: keep one intro-first form, one primary action, one reviewable result card, and one visible export/report path, then verify the same slice with stable Playwright checkpoints before widening navigation.
