# Web-app form -> result export check

Use this when the first generated `web-app` proof now includes one input form, one visible result card, and one obvious export/download action.

Quick pass:

1. The form has one primary action.
2. The result card repeats the same core title or outcome.
3. One export target is visible without opening a second screen.
4. The demo script still matches the proof path.
