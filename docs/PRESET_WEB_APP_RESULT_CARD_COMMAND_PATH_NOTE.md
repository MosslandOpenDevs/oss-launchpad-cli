# Web-app result card command path note

For the first believable web-app demo, keep one setup command, one validation command, and one export path visible on the same result card before adding more actions.
