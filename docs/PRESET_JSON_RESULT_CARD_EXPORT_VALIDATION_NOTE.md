# Preset JSON Result Card Export Validation Note

For the first `web-app` proof slice, keep the result card honest:

- show one visible validation command,
- show one obvious export or download target,
- avoid widening the flow until both cues stay readable together.

This keeps the first browser-facing preset demo aligned with a reproducible maintainer loop.
