# Preset web app Playwright recovery note

Keep the first browser-demo automation recovery-first: verify form inputs, wait for the result card, and on failure recapture the visible state before retrying so the run stays reproducible.
