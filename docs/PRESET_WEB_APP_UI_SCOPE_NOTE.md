# PRESET_WEB_APP_UI_SCOPE_NOTE

Keep the first `web-app` proof small enough to trust:

- one visible landing or result-card slice
- one matching demo script path
- one matching UI checklist section

Do not describe the scaffold as a full product flow until those artifacts expand together.
