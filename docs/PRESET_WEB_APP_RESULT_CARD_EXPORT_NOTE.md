# Preset Web App Result Card Export Note

Use this note when the first preset web-app result card is visible and you need to confirm one download/export path without widening scope.

## Check

- Keep one form input, one visible result card, and one target export file.
- Re-check the export control before changing presets or layout.
- Record the exact file name or artifact label that the reviewer should expect.
