# Preset web-app result-card replay-ready note

For the first web-app result card slice, keep the proof loop small and reproducible:

1. submit one preset-backed input,
2. render one primary result card,
3. export one report artifact,
4. keep the same UI state easy to replay in browser automation.

This matches stable UI/UX-first work and Playwright-friendly verification.
