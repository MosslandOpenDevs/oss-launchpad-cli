# Preset JSON result-card validate export status note

For the first web-app preset demo slice, keep the chooser, one primary action, one stable result card, and one export-ready validation status in the same reviewable bundle. Do not widen the UI flow until that single form-to-card state can show the preset label, validation command, and export path together.
