# governance web-app report_output_handle note

Use `report_output_handle` when one governance web-demo export should keep a handle-style basename for the generated report bundle while the UI slice stays one form, one primary action, and one stable result card.
