# Preset web-app governance UI/Playwright result card note

Keep the first governance web-app slice to one scenario-file input, one primary action, and one export-ready result card, using UI/UX-first layout choices plus Playwright-interactive stability, reproducibility, and recovery checks before widening the demo.
