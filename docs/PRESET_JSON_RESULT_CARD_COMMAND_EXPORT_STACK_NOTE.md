# Preset JSON Result Card Command Export Stack Note

For the first preset JSON result card, keep one narrow proof stack visible:

- setup command
- validation command
- export or download path

That trio keeps the card honest: readers can see how to start, how to verify, and what artifact leaves the flow.
