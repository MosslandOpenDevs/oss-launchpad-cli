# Preset web-app form -> result card stack

Use this note before widening the first `web-app` proof beyond the initial happy-path slice.

Keep the first demo lane narrow:
- one primary form,
- one reviewable result card,
- one visible download or handoff target.

Only add extra screens after that stack is reproducible from the landing brief, UI checklist, and demo script.
