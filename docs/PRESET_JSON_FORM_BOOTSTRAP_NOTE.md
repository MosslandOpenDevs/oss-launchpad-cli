# preset JSON form bootstrap note

Use exported preset JSON as the first source of truth for form defaults.

That keeps the first web form slice aligned with the CLI scaffold by reusing:

- preset labels,
- preset ids, and
- starter descriptions

from one deterministic export instead of retyping them in the UI.
