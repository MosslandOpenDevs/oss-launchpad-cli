# Web-app result card validation HUD note

Keep the first generated web-app result card honest: show one primary action, one validation command, and one export/download target in the same visible card.

Use this when the first browser demo should feel reviewable without widening into multiple panels, hidden actions, or secondary flows.
