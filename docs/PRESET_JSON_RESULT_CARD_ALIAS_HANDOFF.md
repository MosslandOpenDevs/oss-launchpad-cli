# Preset JSON result-card alias handoff

Keep the governance preset path small and reviewable: one preset JSON input, one reproducible web result card state, and one exported report bundle with the same basename/label shown across the CLI and UI handoff.
