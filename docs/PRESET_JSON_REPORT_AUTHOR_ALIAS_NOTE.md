# Preset JSON report-author alias note

If a preset metadata fixture already ships `report_author` or `report_authors`, keep those aliases visible in the exported owner field instead of rewriting the preset by hand.

Proof lane:
- one preset fixture
- one JSON export
- one green unittest pass before push
