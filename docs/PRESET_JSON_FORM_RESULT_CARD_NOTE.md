# Preset JSON form result card note

Start with one exported preset payload, one setup form, and one visible result card. That keeps the first web-app proof narrow enough to validate quickly while still showing a believable end-to-end scaffold story.
