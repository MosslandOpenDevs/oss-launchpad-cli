# Preset web app result card export review note

For the first believable web-app proof, keep one stable result card tied to one export review pass:

1. submit one preset-backed form path
2. confirm the result card labels and primary action
3. review the exported proof path before widening UI scope

Use this note when UI/UX and Playwright stability rules need a single maintainer-facing checkpoint.
