# Preset web-app result-card validate command note

For the first governance web demo slice, keep one preset chooser, one visible result card, and one explicit local validation command together before expanding the UI.
