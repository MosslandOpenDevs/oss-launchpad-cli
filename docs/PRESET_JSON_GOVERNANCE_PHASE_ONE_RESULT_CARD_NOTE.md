# Preset JSON governance phase-one result-card note

For the first browser-facing preset slice, keep the governance-aligned order explicit: scenario-file/report-first planning upstream, then one chooser, one primary action, and one reviewable result card in the web-app handoff.

Keep the first result card small enough to validate and replay before widening exports or secondary navigation.
