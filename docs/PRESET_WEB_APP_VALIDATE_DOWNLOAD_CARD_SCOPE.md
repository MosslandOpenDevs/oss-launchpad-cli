# Preset web-app validate + download card scope

Keep the first believable web-app preset demo narrow.

- one input form
- one primary submit action
- one reviewable result card
- one visible validation command
- one downloadable proof path

Use this scope before widening the demo. It keeps the UI/UX-first slice simple and gives Playwright one stable form-to-card-to-download lane to replay.
