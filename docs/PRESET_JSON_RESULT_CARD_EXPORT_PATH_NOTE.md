# Preset Json Result Card Export Path Note

- Keep one small, replayable proof slice.
- Show the scenario or preset source plus the generated output path together.
- Re-run the smallest relevant validator before push.
