# Web-app result card download validate note

Keep the first web-app result-card slice tied to one visible export path and one rerunnable validation command before widening the UI.

## Checklist
- Confirm the first result card shows one primary status line.
- Confirm the export/download control points to one stable file path.
- Re-run the preset proof command before pushing README or template changes.
- Keep extra secondary actions out of scope until the first download path is reviewable.
