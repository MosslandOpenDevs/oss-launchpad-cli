# Preset web-app form result recovery note

Keep the first web-app proof scoped to one form submission, one visible result card, and one reproducible recovery step when the run flakes.

Use this note when the web-app preset needs a lightweight reminder that browser-proof stability matters more than adding a second screen too early.
