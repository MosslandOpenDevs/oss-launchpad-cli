# Governance scenario input precedence note

When the generated web-app starter is paired with governance-sandbox phase-one work, keep the first proof lane honest: nested scenario input proposal text should populate the result card before wrapper titles or scenario labels, and commit/push should wait for a green local rerun.
