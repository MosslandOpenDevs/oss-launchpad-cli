# Preset JSON Web-App Single Preset Note

When the first UI proof only needs one chooser lane, use:

```bash
PYTHONPATH=src python3 -m oss_launchpad_cli.cli presets --json --preset web-app
```

This keeps the first form/result-card prototype focused on one preset payload before broadening to a multi-preset picker.
