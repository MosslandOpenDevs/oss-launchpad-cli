# Preset JSON result card validate/export badge note

For the first web-app chooser/result-card slice, keep one visible badge pair on the card:

- the preset validation command,
- the export/download target.

That keeps the UI review aligned with a UI/UX-first primary action and a Playwright-friendly proof checkpoint.
