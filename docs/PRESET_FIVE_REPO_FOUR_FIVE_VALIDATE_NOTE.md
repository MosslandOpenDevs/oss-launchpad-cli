# Preset five-repo four-five validate note

For recurring five-repo maintenance passes, keep `oss-launchpad-cli` and `governance-sandbox` visible in every report. Only claim commit/push for either repo after its own local validation passes, even when the broader update stays intentionally short.
