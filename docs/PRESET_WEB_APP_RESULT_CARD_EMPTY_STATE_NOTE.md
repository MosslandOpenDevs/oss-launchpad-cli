# Preset web-app result card empty-state note

Before real demo data exists, keep the first result card honest and calm.

The empty state should show:

- one short status label,
- one sentence explaining what input is still needed,
- and one obvious next action.

Do not fake success metrics or filled charts in the empty state.
