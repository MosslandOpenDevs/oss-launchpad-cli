# Preset Web App Result Card Recovery Badge Note

When the first web-app result card is flaky, keep one visible validation badge and one retry path so the recovery loop stays reviewable instead of hidden.
