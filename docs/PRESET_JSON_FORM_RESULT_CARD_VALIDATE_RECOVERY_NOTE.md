# Preset JSON form result-card validate recovery note

For the first web-demo slice, keep the loop tiny: validate the preset-backed form, confirm the primary action, and recover by replaying the same result-card path before widening the UI.
