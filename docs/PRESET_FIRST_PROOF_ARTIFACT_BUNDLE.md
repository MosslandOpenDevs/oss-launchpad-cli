# Preset first proof artifact bundle

When the first believable proof is ready, keep the bundle small and reviewable:

- one visible proof asset
- one reproducible check asset
- one short status line that does not oversell maturity

Treat the bundle as a review package, not a roadmap dump.
