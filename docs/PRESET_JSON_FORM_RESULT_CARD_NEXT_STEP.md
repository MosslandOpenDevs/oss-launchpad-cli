# preset json form result-card next step

Use this note when the first `presets --json` browser proof already shows one setup form and one result card, and the next step is to keep the follow-up action obvious.

- Keep one primary next step on the result card.
- Point that action to the matching generated proof asset or review command.
- Re-run the preset JSON and web-app tests before widening the flow.
