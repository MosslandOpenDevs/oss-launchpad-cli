# Preset JSON result-card download gate

When a chooser or tiny web demo consumes `oss-launchpad presets --json`, keep the first visible proof to one form, one primary action, one result card, and one explicit download/export path. Validate that narrow slice before widening the flow.
