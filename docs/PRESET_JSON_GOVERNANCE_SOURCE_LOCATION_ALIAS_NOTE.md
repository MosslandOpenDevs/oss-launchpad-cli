# Preset JSON governance source_location alias note

For the first governance web-demo/result-card slice, allow imported scenario bundles to keep their source metadata under `source_location` and map it into the same report/output lane before widening UI scope.
