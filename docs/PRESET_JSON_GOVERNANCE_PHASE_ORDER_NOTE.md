# Preset JSON governance phase-order note

For governance-sandbox-aligned preset work, keep the delivery order visible:

1. scenario file import stays proven first
2. markdown/html/json report bundle stays proven second
3. preset catalog wiring can expand next
4. web app form + result card follows after preset proof

UI/UX cue: keep the first visible screen scoped to one chooser, one primary action, and one reviewable result card.
Playwright cue: keep the first browser proof reproducible before widening export or multi-card flows.
