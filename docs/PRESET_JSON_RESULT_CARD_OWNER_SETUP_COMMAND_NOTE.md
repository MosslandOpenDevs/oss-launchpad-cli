# Preset JSON result card owner + setup command note

For the first preset chooser/result-card slice, keep the preset label, named owner, and visible setup command together so the primary next step is obvious before adding more actions or secondary metadata.
