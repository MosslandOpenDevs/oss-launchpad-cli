# Preset JSON form result card stack

Use this note when the **web-app** preset should show a single proof stack from **JSON form input** to **result card output** to **exported artifact review**.

Keep the first proof path explicit:

- fill one JSON form
- render one result card
- export one proof artifact bundle
- keep the primary review command visible

This helps maintainers confirm the demo flow before widening scope.
