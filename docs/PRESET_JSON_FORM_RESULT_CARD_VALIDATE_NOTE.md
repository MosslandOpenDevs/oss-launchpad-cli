# Preset JSON form result-card validate note

Keep the first `presets --json` web slice narrow: one setup form, one primary result card, and one validation rerun before widening export or navigation scope.
