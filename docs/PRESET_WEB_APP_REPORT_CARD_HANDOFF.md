# Preset web-app report card handoff

Use this note when the generated `web-app` scaffold is being shaped into a first believable report-style demo.

Keep the first proof narrow:
- one input form
- one result/report card
- one downloadable artifact or bundle target
- one demo command that can be rerun without explanation
