# preset web-app form-card proof note

For the first web-app preset UI slice, keep one form, one primary action, one stable result card, and one review command visible together before widening the flow or browser automation surface.
