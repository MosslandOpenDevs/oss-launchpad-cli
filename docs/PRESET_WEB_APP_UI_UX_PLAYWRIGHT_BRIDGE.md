# Preset web-app UI/UX + Playwright bridge

Keep the first web-app proof limited to one preset chooser, one intro-first primary action, and one reviewable result card, then verify the same slice with stable Playwright checkpoints before widening coverage.
