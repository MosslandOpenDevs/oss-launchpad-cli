# Preset web-app result card scan note

Before widening the generated `web-app` demo, keep the first visible result card easy to scan.

Quick scan:

1. Keep one dominant headline.
2. Keep one proof action or download target.
3. Keep supporting metadata grouped instead of scattered across multiple cards.
4. Re-run the same first-result-card proof after copy edits.
