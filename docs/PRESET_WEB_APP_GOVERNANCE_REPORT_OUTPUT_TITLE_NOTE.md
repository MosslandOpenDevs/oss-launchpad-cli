# Preset web-app governance report-output-title note

For the first governance web demo slice, allow `report_output_title` to name the exported report bundle while keeping the UI/UX-first and Playwright-style loop the same: one form, one primary CTA, one stable result card, one validation replay before commit/push.
