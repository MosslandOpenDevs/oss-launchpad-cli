# Preset web-app result-card export rule

Use this rule when the first visible web-app proof already reaches one result card and one export target.

- Keep one primary result card visible above the fold.
- Keep one obvious export or download target tied to that card.
- Do not widen the demo to multiple cards until the first export path is reproducible.
