# Preset JSON form preset-catalog note

Keep the first JSON preset form wired to one exported preset catalog so field labels, summaries, and downstream result-card copy stay aligned. Treat the catalog as the single source of truth before adding extra form-side wording.
