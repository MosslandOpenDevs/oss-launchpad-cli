# Preset JSON Form Result Card Export Stack

For the first web-app chooser slice, keep the stack narrow and obvious:

1. one preset selector,
2. one result card with starter docs and validation command,
3. one export/download proof for the same selected preset.

This keeps the UI/UX slice reviewable and lines up with stable Playwright checkpoint coverage.
