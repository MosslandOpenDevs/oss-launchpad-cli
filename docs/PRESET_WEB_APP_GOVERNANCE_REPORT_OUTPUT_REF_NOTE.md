# preset web-app governance report output ref note

For the first governance web-app result-card slice, keep `report_output_ref` treated as the same shared report basename that points to the exported governance JSON/Markdown/HTML bundle.
