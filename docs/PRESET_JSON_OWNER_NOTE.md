# Preset JSON owner note

`oss-launchpad presets --json` is useful for pickers and setup forms, but the consuming UI should still keep one clear owner for the first proof lane.

When wiring the JSON into a chooser, preserve three cues together:
- preset label,
- preset summary,
- and the maintainer or team expected to customize the first generated proof.
