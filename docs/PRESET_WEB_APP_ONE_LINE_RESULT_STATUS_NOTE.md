# Preset web app one-line result status note

Keep the short maintainer report to one line per active repository with the repo name, change state, validation result, and commit/push state so the web-app result-card work still stays easy to scan.
