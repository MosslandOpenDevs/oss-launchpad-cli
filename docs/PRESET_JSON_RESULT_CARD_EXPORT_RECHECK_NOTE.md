# Preset JSON result-card export recheck note

Keep the first web-app demo slice small:

1. one preset chooser or input form
2. one primary action
3. one reviewable result card
4. one visible export or download target
5. one validation rerun before commit or push

Use this note when the first browser-facing proof needs to stay aligned with UI/UX-first structure and reproducible Playwright-style rechecks.
