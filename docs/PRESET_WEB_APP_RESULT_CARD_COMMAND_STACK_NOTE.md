# Preset web-app result card command stack note

Use this note when the first believable `web-app` demo slice already has one form and one result card.

Keep one small command stack visible on that result card:

- one setup command,
- one validation command,
- and one export target.

If any of those moves off-screen or into a secondary panel, narrow the slice again before widening the demo flow.
