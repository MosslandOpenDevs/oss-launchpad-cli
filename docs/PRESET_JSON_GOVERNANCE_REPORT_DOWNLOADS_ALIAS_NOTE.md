# Preset JSON governance report downloads alias note

For the first governance-aligned web-app slice, keep preset JSON and result-card copy compatible with `report.outputs.downloads` plus top-level `report_downloads` so one scenario file can still advertise downloadable JSON, Markdown, and HTML reports without extra reshaping.
