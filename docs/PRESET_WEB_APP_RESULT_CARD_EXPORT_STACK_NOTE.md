# Preset web-app result card export stack note

For the first web-app proof slice, keep one form submit, one result card, and one visible export target in the same review path so UI/UX polish and Playwright replay stay grounded in a single reproducible stack.
