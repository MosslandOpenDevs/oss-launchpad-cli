# Preset web-app result card validation export bridge

Keep the first web-app result card focused on one visible validation command plus one visible export path so the browser demo stays reviewable before widening beyond the first stable slice.
