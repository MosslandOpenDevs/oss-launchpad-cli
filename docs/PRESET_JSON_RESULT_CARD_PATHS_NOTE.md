# Preset JSON result card paths note

When wiring `oss-launchpad presets --json` into a small UI, keep the preset summary, validation command, and generated path cues visible on the same result card.
That makes the first form submit feel reviewable before widening the demo beyond one chooser and one result panel.
