# Preset JSON form single proof rule

Keep the first browser-facing proof narrow: one preset chooser, one generated result card, and one visible validation command that matches the exported preset metadata.
