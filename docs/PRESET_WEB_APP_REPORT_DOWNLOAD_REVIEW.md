# Preset web-app report download review

For the first web-app proof slice, keep the maintainer review loop limited to one form/result-card flow plus one report-download check.

Quick review:
- run the printed proof command,
- verify the result card keeps the download action visible,
- reopen one generated report before widening the UI surface.
