# PRESET WEB APP FIRST RESULT CARD CHECK

Use this after the first `web-app` UI slice is visible.

Keep the proof small and reviewable:

- confirm one result card is visible from the main happy path,
- confirm the landing-page brief still matches that result card,
- confirm `demo/run_demo.sh` still walks through the same proof,
- defer secondary screens until this first result card is believable.
