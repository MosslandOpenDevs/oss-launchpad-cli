# Preset JSON result-card empty-state note

When the first web-app preset demo has not selected a preset yet, keep the result area as a calm empty state that explains the next action in one sentence.

Do not widen the first slice beyond one chooser, one primary action, and one stable result card. The empty state should make the primary CTA obvious without adding extra navigation noise.
