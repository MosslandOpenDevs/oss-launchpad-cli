Keep the first governance web-app proof narrow: one stdin-fed YAML/JSON scenario replay, one primary action, one stable result card, and one exported report bundle.

Apply the same UI/UX-first and Playwright-interactive review rules before widening beyond this form-to-card slice.
