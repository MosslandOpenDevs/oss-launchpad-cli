# Preset JSON Result Card Status + Export Note

For the first web result-card slice, keep the UI promise narrow:

1. one visible validated status line
2. one obvious export path
3. no extra secondary actions until the core flow is reproducible

That keeps the preset-to-report proof small enough to verify quickly.
