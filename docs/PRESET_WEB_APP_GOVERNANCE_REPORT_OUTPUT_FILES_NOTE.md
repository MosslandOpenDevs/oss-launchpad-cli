# PRESET_WEB_APP_GOVERNANCE_REPORT_OUTPUT_FILES_NOTE

For the first governance web-demo/result-card slice, treat `report.outputs.files` as the same stable export cue as `report_output_dir` or `reports_directory`: one scenario file, one primary action, one reviewable result card, one visible report bundle.

Keep the UI/UX-first and Playwright-style loop narrow and replayable before adding more screens or actions.
