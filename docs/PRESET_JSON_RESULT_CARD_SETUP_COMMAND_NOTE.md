# Preset JSON result card setup command note

Keep the first preset-JSON result card explicit about both the initial setup command and the follow-up validation command so a maintainer can go from chooser to runnable proof without reopening the CLI help.
