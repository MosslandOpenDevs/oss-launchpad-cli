# Preset JSON governance scenario source short alias note

When the web-app preset grows toward governance-sandbox report demos, keep exported preset JSON compatible with `scenario_src` and `source_href` so one form submission can preserve the imported scenario source path or URL in the result-card-ready report bundle.
