# Preset web-app result card validate/export start

Keep the first believable `web-app` result card narrow:

- show the chosen preset label
- show one visible **validation command**
- show one visible **export / download target**
- avoid adding extra secondary actions before this path is stable

This keeps the UI slice aligned with a one-form -> one-result-card -> one-export proof loop.
