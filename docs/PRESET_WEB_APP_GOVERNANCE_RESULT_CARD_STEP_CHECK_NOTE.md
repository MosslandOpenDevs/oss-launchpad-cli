# Preset web-app governance result-card step check note

Keep the first governance-aligned `web-app` preset demo reproducible as a small step-checked slice.

1. Open one input form.
2. Trigger one unmistakable primary action.
3. Verify one result card with validation/export cues.
4. Re-run the same narrow validation before claiming progress.
