# Preset JSON form one-flow rule

Keep the first preset chooser intentionally narrow:

1. one preset-selection form,
2. one visible result card,
3. one explicit next-step artifact or command.

Do not widen the demo until that single form-to-card-to-artifact path is reproducible.
