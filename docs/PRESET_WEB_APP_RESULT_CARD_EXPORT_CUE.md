# Preset web-app result-card export cue

For the first governance-aligned `web-app` slice, keep the UI proof narrow:

1. one preset chooser or form
2. one obvious primary action
3. one stable result card
4. one visible export/download cue

Validate that slice locally before commit/push so the result-card claim stays reproducible.
