# Preset web-app result card one-glance note

Keep the first generated web-app result card readable in one glance: show the primary status, the chosen preset label, and one obvious download or export target above the fold before adding secondary actions.
