# preset web-app result-card export recovery note

For the first web-app proof, keep the scope to one form, one primary action, one stable result card, and one visible export/download target.

If the browser proof drifts, recover by replaying the same smallest form-to-card flow before widening UI states or secondary actions.

Validate the preset output again before commit/push.
