# PRESET_JSON_RESULT_CARD_SETUP_COMMAND_ALIAS_NOTE

Use `oss-launchpad presets --json --preset web-app` when the first result-card UI pass needs the same setup lane exposed as `result_card_setup_command`, `setup_command`, and `customize_first_command`.
