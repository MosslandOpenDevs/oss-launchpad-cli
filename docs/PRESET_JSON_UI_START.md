# Preset JSON UI start

Use this note when `oss-launchpad presets --json` is feeding the first tiny chooser, setup form, or review bot and you only need the shortest believable UI handoff.

- keep the picker tied to the exported label + summary fields
- keep the first screen scoped to one preset choice and one next action
- widen the flow only after the JSON export contract is validated
