# Preset JSON Web Form UI/UX + Playwright Bridge

Use this note when the first browser demo grows from exported preset JSON into a visible chooser flow.

Keep the proof lane small:
- one preset chooser
- one result card
- one visible validation command

Review the copy and above-the-fold hierarchy first so the chosen preset is obvious without scrolling. Then lock the same slice behind stable selectors, deterministic fixture data, and a single replayable Playwright path before widening to multi-step setup flows.
