# Preset JSON result card report path note

Keep the first believable web-app slice small: one preset chooser, one visible result card, and one obvious exported report path.

Review rule:
- do not add secondary actions until the result card already shows one clear output path,
- keep the validation command visible next to that exported path,
- and use the same path in demo proof, README copy, and Playwright recovery notes.
