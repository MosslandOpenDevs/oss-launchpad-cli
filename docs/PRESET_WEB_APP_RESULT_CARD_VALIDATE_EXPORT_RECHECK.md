# Preset web-app result-card validate-export recheck

Use one narrow recheck before calling the first web-app slice ready.

- Keep the first browser-facing proof to one chooser, one primary action, and one reviewable result card.
- Keep the validation command visible beside the export/download handoff.
- Re-run the smallest validation/export check before commit or push.
