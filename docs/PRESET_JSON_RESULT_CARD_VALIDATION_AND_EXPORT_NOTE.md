# preset JSON result card validation and export note

For the first preset-picker result card, keep the validation command and the export/download target visible together.

That makes the first browser or maintainer proof stay honest: one chosen preset, one visible validation command, and one explicit handoff path.
