# Preset JSON result-card proof note

When turning `oss-launchpad presets --json` into a tiny browser or setup demo, keep the first proof lane minimal:

1. one preset chooser,
2. one result card with preset label + validation command,
3. one visible starter-doc list,
4. one reproducible review command.

This keeps the first UX slice easy to verify and easy to replay.
