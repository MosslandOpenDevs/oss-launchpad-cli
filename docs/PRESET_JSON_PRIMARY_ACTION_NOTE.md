# Preset JSON primary action note

Expose one `primary_action` field in `oss-launchpad presets --json` when a chooser, setup form, or result card should highlight the single most obvious next command without reconstructing it from longer metadata keys.
