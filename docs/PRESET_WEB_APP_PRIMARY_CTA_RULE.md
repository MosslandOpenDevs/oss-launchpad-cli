# PRESET_WEB_APP_PRIMARY_CTA_RULE

Keep the first `web-app` preset proof centered on one unmistakable primary CTA that leads from the opening form to one reviewable result card. Add secondary actions only after the primary CTA, validation command, and export path are already stable.
