# Preset Governance Proposal File Alias Note

Keep repo-4 preset handoffs aligned with repo-5 scenario work by naming the direct `proposal_file`, `proposal_path`, and `proposal_src` aliases when proposal copy should load from a local file before report generation.
