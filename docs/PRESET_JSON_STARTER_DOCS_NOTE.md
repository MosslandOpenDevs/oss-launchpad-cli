# Preset JSON starter docs note

When a preset picker renders JSON metadata, keep the generated starter docs visible beside the preset label or summary.
That helps maintainers understand the first proof assets they will get before they leave the chooser or result card.
