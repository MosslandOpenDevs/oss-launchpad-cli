# Web-app result-card command/export rule

Keep the first `web-app` result-card proof limited to one visible validation command and one explicit export target so UI proof stays reviewable, reproducible, and honest.
