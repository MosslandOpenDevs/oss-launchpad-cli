# Preset web-app result card report-ready note

Keep the first generated `web-app` proof honest: one form, one visible result card, and one report-ready download or handoff target.

Use this note when you want a short review pass before widening the demo beyond the first result card.
