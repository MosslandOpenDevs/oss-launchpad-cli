# Preset web-app result card report-ready note

For the first believable `web-app` proof, keep the result card tied to one report-ready download target before adding extra actions, filters, or secondary cards. One stable card plus one explicit export path is enough for the first review loop.
