# Preset JSON result card validation badge

When a preset JSON powered setup form renders a result card, keep one validation badge visible near the primary action.

The badge should name the preset-specific validation command so the first follow-up action stays reproducible instead of turning into generic UI copy.
