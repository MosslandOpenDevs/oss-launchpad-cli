# Preset web-app result card one primary path

For the first generated `web-app` proof slice, keep one obvious path visible:

1. one input form,
2. one result card,
3. one primary download or handoff target.

Use this when tightening README copy or generated demo guidance so the first UI proof stays small and reproducible.
