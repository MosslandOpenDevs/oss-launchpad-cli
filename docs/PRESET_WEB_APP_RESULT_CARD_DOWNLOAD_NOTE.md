# Preset web-app result card download note

Use this when the first web demo result card needs one clear downloadable proof target.

- Keep the primary status visible before secondary actions.
- Offer one obvious download/report action before adding extra controls.
- Reuse the same exported filename in the card copy and validation notes.
- Treat the first form -> result card path as the default proof slice for UI review.
