# Preset web-app governance report bundle entry note

Keep the governance web demo narrow: one scenario file entry path, one primary action, one stable result card, and one exported report bundle.

Review the slice with UI/UX-first copy and layout decisions, then replay it with Playwright-style stability checks before widening the flow.
