# Preset web-app result card export label note

Use this when the first `web-app` preset result card should stay easy to scan while still proving one export-ready path.

Keep the UI slice narrow:
- show one preset label clearly
- keep one validation command visible
- keep one export/download path visible on the same card
- do not widen beyond one form and one reviewable result card until the validator stays green
