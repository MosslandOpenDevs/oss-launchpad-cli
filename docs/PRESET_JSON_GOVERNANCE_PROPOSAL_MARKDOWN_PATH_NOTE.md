# Preset JSON governance proposal markdown path note

For the repo-4/repo-5 bridge, keep preset-json and web-card planning aligned with a scenario-file input lane that can load proposal markdown from `proposal_markdown_path`, `proposal_md_path`, or `proposal_copy_path`. Preserve the scenario-file -> report-bundle -> result-card order and validate before push.
