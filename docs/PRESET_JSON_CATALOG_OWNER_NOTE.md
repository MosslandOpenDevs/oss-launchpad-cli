# Preset JSON catalog owner note

Keep the preset JSON catalog owner-friendly: the first exported preset list should surface each preset summary and starter assets clearly enough that a maintainer can choose one without opening template files first.
