# Preset first proof validate status bridge

After one preset-specific proof edit lands, rerun the matching validation command first and only then write the shortest public-ready status line for that preset slice.
