# Preset web-app result-card progress badge gate

Keep the first `web-app` proof scoped to one honest progress badge on one result card, paired with the visible validation command and export target before widening UI states.
