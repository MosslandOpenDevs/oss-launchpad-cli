# Preset JSON result card validate-export gate

For the first web-app proof, keep one form, one primary action, and one reviewable result card. Do not present export/download as ready until the matching validation command is visible and reproducible.
