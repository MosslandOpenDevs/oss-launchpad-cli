# Preset JSON form result-card validation start

For the first preset-driven browser flow, keep the exported validation command visible on the same result card that shows the chosen preset.

Minimum proof:
- one preset chooser fed by `oss-launchpad presets --json`
- one result card that repeats the preset label
- one visible validation command for the generated scaffold
- one follow-up action that points to the first believable proof
