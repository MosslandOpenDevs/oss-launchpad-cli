# Preset JSON result-card output slug note

When a web-app setup flow exports a starter bundle, prefer one visible output slug that matches the result-card label and the saved artifact handoff.

This keeps the first chooser -> result-card -> export proof narrow, stable, and easy to replay in browser checks.
