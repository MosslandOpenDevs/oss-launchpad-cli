# Preset First Proof Validate Then Push

After customizing the first preset-specific proof asset, rerun the matching validation command before pushing the scaffold update.

Minimum maintainer loop:
- customize one believable proof file,
- rerun the preset validation command,
- push only after the proof command stays green.
