# Preset JSON result card export owner status note

When the first preset chooser flow saves or exports proof, keep one short owner-ready status on the result card beside the preset label and validation command.

That makes the first web-app proof easier to review without widening the UI beyond one chooser, one action, and one result card.
