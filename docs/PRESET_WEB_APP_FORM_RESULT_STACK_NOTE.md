# Preset web-app form-to-result stack note

Use this note when the first web demo slice should stay limited to **one form submission, one result card, and one report handoff**.

Keep the stack stable:
- review the form fields before styling extra screens;
- verify the first result card exposes the main decision summary;
- confirm the report handoff stays visible from that same result state.

This keeps the first web-app proof aligned with UI/UX review and reproducible browser checks.
