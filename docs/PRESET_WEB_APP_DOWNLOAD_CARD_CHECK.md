# Preset web-app download card check

For the first `web-app` demo slice, keep the result card honest:

1. show the selected preset clearly
2. keep generated files or commands visible as explicit download/replay targets
3. verify the empty/loading/error states before widening the card
4. re-run the smallest preset validation proof before push

This keeps UI polish tied to reproducible scaffold output instead of a screenshot-only demo.
