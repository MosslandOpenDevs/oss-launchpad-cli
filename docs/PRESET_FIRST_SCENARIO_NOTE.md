# Preset first scenario note

Use this note after `oss-launchpad init` when a maintainer wants one concrete proof-of-life task instead of a long backlog.

## By preset

- `ai-agent`: run the demo once and save the first prompt/eval pair that proves the agent contract is real.
- `web-app`: capture one landing-page screenshot and one successful happy-path run note before expanding scope.
- `python-lib`: implement one importable function, run the smoke test, and show the example output in `examples/basic_usage.py`.

Keep the first scenario small enough to finish in one sitting.
