# PRESET_WEB_APP_PLAYWRIGHT_STEP_CHECK

Keep the first web-app browser proof reproducible.

- Validate one form input path before widening coverage.
- Confirm one primary action and one visible result card.
- Recover from failure by rechecking the same step order instead of widening the script.
