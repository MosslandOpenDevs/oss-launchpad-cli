# Preset web-app result card report stack

Keep the first believable web-app proof narrow:

- one form submission
- one visible result card
- one report file reference
- one download/review check

Do not add secondary panels before the result card and report handoff stay reproducible together.
