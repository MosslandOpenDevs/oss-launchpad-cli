# Preset JSON result-card export trio note

For the first web-app preset proof, keep one chooser submit tied to one visible result card and one export trio:

- chosen preset label,
- validation command,
- exported bundle target.

If the first browser slice cannot show those three things together, narrow the UI before adding more actions.
