# Eval Notes

Use this directory for deterministic evaluation fixtures and replay-oriented notes that validate CLI output, preset metadata, and generated artifacts.

Prefer small fixture sets that can be rerun quickly before commit/push.
